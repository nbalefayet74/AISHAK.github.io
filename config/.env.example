# ------------------ Application ------------------
APP_NAME="AISHAK"
APP_ENV=local           # local | staging | production
APP_DEBUG=true
APP_URL="http://localhost"

APP_TIMEZONE="Europe/Paris"
APP_LOCALE="fr_FR.UTF-8"

# ------------------ Base de données --------------
DB_HOST=localhost
DB_NAME=aishak
DB_USER=root
DB_PASS=secret

# ------------------ Mail / SMTP ------------------
MAIL_DRIVER=smtp        # smtp | mail
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_SMTPAUTH=true
MAIL_SECURE=tls         # tls | ssl | false
MAIL_USERNAME=no-reply@example.com
MAIL_PASSWORD=secret
MAIL_FROM_ADDRESS=no-reply@example.com
MAIL_FROM_NAME="AISHAK"

# ------------------ Divers -----------------------
# Exemple d’API :
STRIPE_KEY=pk_test_***
STRIPE_SECRET=sk_test_***
